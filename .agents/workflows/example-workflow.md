---
description: An example workflow to demonstrate the syntax.
---

### Example Workflow

1. Start by defining the goal of this workflow.
2. Outline the steps necessary. For example:

```
// turbo
run_command: echo "Hello World"
```

3. Use the `// turbo` annotation for steps that should run automatically.
